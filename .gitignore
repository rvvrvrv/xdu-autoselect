# 运行时 / 个人数据 —— 不要提交
config.json
state.json
*.log
monitor-log.jsonl
snapshots/
.edge-profile/
node_modules/
runtime/node.exe

# 系统 / 编辑器
.DS_Store
Thumbs.db

# 认证 / 备份临时文件
*.bak

# 本地站点镜像（学校系统内容 + 登录态，绝不入库）
dead-site/
.debug-profile/
.browser-profile/

# 打包产物
dist/

