此文件夹用于放置「便携版 Node.js」，让朋友无需安装任何东西、双击即用。

方法（由分享者/你来做一次）：
  1. 到 https://nodejs.org 下载 Windows x64 的 zip 版（不是 msi）。
  2. 解压后，把里面的 node.exe 复制到本文件夹 runtime\ 下。
  3. 重新打包整个项目文件夹（含 node.exe 和 node_modules）发送给朋友。

放到 runtime\node.exe 后，一键启动.bat 会优先用它，朋友电脑完全不用装 Node。
